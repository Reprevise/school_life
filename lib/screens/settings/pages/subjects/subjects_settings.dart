import 'package:flutter/material.dart';

class SubjectsSettingsPage extends StatelessWidget {
  const SubjectsSettingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      extendBodyBehindAppBar: true,
      body: ListView(),
    );
  }
}
